[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
postcard-rpc = { version = "0.11.9" }
postcard-schema = { version = "0.2.1", features = ["derive"] }
serde = { version = "1.0", default-features = false, features = ["derive"] }

[features]
use-std = []
